# Phase 2 transitional routes: TLS on Envoy, backends stay on legacy hosts.
# DNS A records → 192.168.5.21 (infrastructure/lab.yaml dns.app_hosts).
# Uses Envoy Gateway Backend CRD (Argo excludes EndpointSlice by default).
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: jellyfin
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.9
        port: 8096
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - jellyfin.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: jellyfin
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: qbittorrent
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.9
        port: 8085
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: qbittorrent
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - qbittorrent.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: qbittorrent
---
# sonarr.lab = anime (primary); sonarr-tv.lab = TV
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: sonarr
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.9
        port: 8990
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - sonarr.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: sonarr
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: sonarr-tv
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.9
        port: 8989
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr-tv
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - sonarr-tv.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: sonarr-tv
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: prowlarr
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.9
        port: 9696
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - prowlarr.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: prowlarr
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: homeassistant
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.2.8
        port: 8123
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: homeassistant
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - homeassistant.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      # HA returns 400 if it sees X-Forwarded-* without trusted_proxies.
      # Strip until HA is configured with use_x_forwarded_for + trusted_proxies.
      filters:
        - type: RequestHeaderModifier
          requestHeaderModifier:
            remove:
              - x-forwarded-for
              - x-forwarded-proto
              - x-forwarded-host
              - x-forwarded-port
              - x-real-ip
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: homeassistant
---
# Pi-hole UI — Envoy Backend directly to LXC (no sidecar proxy).
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: pihole
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.1.11
        port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pihole
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - pihole.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: pihole
---
# scarif Unraid UI — LE on Envoy; Unraid stays HTTP on :80.
# NFS/iSCSI clients must use 192.168.5.10 (not scarif.lab — that is Envoy).
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: Backend
metadata:
  name: scarif
  namespace: transitional
spec:
  endpoints:
    - ip:
        address: 192.168.5.10
        port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: scarif
  namespace: transitional
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: lab
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - scarif.lab.jacobdrury.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: scarif
