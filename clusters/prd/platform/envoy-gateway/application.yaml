# Official Envoy Gateway → Argo CD install pattern:
# https://gateway.envoyproxy.io/docs/install/install-argocd/
#
# Chart source matches upstream (docker.io/envoyproxy / gateway-helm / SSA).
# Extra Git sources: Helm values + Gateway/Certificate/HTTPRoutes (GitOps stand-in
# for kubectl apply of quickstart.yaml).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: envoy-gateway
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  sources:
    - repoURL: docker.io/envoyproxy
      chart: gateway-helm
      targetRevision: v1.9.1
      helm:
        releaseName: eg
        valueFiles:
          - $values/clusters/prd/platform/envoy-gateway/values.yaml
    - repoURL: https://github.com/jacobdrury/homelab.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/jacobdrury/homelab.git
      targetRevision: main
      path: clusters/prd/platform/envoy-gateway
      directory:
        include: resources.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: envoy-gateway-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
