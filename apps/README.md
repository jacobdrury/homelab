# Apps

Workload charts live under [`clusters/prd/apps/`](../clusters/prd/apps/) (Argo).
Source notes / compose references can live here later (`media/`, `home/`, …).

| Path | Role | Owner |
|------|------|-------|
| (cluster) `clusters/prd/apps/homepage/` | Dashboard | Argo |
| `media/`, `home/`, `games/`, `network/` | Workload notes (Phase 3+) | Argo when added |

Platform (CNI, CSI, secrets, Argo, cert-manager, Envoy): [`clusters/prd/platform/`](../clusters/prd/platform/).

Bootstrap / DR: [bootstrap](../bootstrap/README.md).
